<head>
<meta property="og:title" content="localhost:3000" />
<meta property="og:type" content="band" />
<meta property="og:url" content="http://mighty-refuge-6115.herokuapp.com/artists" />
<meta property="og:image" content="" />
<meta property="og:site_name" content="Three Repeater" />
<meta property="fb:admins" content="513047468" />
</head>

<div id="fb-root"></div>\
<script>(function(d, s, id) {
	var js, fjs = d.getElementsByTagName(s)[0];
	if (d.getElementById(id)) return;
	js = d.createElement(s); js.id = id;
	js.src = "//connect.facebook.net/en_US/all.js#xfbml=1";
	fjs.parentNode.insertBefore(js, fjs);
}(document, 'script', 'facebook-jssdk'));</script>

<h1>Listing artists</h1>

<table>
  <tr>
    <th>ID </th>
    <th>Name</th>
    <th>Url Slug</th>
    <th>City</th>
    <th>Province</th>
    <th>Influence</th>
    <th>Bio</th>
    <th>Contact info</th>
    <th>Date founded</th>
    <th>Created date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 <% @artists.each do |artist| %>
  <tr>
      <td> <%=artist.id%> </td>
     <td><%= artist.name %></td>
    <td><%= artist.url_slug %></td>
    <td><%= artist.city %></td>
    <td><%= artist.province %></td>
    <td><%= artist.country %></td>       ``
    <td><%= artist.influence %></td>
    <td><%= artist.bio %></td>
    <td><%= artist.contact_info %></td>
    <td><%= artist.date_founded %></td>
    <td><%= artist.created_date %></td>

    <td><%= link_to 'Show', artist_link_path(artist.url_slug) %></td>
    <% if can? :update, artist %>
        <td><%= link_to 'Edit', edit_artist_path(artist.url_slug) %></td>
        <td><%= link_to 'Admin', artist_admin_path(artist.url_slug)%></td>
        <td><%= link_to 'Destroy', artist, :method => :delete, :confirm => 'Are you sure?' %></td>
        <td><%= link_to 'Add Song', add_song_path(artist.url_slug) %></td>
    <% end %>
 </tr>

<% end %>
</table>


</br>

<% if can? :create, @artist %>

      <%= link_to 'New Artist', new_artist_path %>
  <% else %>

    <%= link_to "sign in", new_user_session_path %> or <%= link_to "sign up", new_user_registration_path %> to Create and Artist </br>

<% end %>


<%= link_to 'Song List', songs_path %>

<br/>
<br/>
<br/>



<div class="fb-like" data-href="http://localhost:3000/artists" data-send="false" data-width="450" data-show-faces="true"></div>

