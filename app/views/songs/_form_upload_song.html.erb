<h2>Upload a new MP3 song</h2>

<%= form_tag upload_path,:method => "post", :multipart => true do  %>

    <h2>Ready!!!  <%= @id %></h2>
    <%= file_field_tag "mp3file" %>
    <%= submit_tag "Upload" %>
    <input id="song_id" name="song_id" type="hidden" value=<%=@id %> />
<% end %>