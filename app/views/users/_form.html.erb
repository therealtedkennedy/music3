<%= form_for @user, :url => url_for(:controller => 'users', :action => 'boo'), :validate => true do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song .errors.count, "error") %> prohibited this song  from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%=f.label :first_name %>
    <%=f.text_field :first_name,:class=>"span12" %>
  </div>

   <div class="field">
    <%=f.label :last_name %>
    <%=f.text_field :last_name,:class=>"span12" %>
  </div>

  <div class="field">
    <%=f.label :Email %>
    <%=f.text_field :email,:class=>"span12" %>
  </div>

  <div class="field">
    <%=f.label :pay_pal_email %>
    <%=f.text_field :pay_pal_email,:class=>"span12"%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>